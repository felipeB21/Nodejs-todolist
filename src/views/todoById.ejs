<!DOCTYPE html>
<html lang="en">
  <%- include ('./partials/head') %>
  <body>
    <%- include ('./partials/header') %>
    <div class="b-width">
      <div class="mt-20">
        <div class="bg-li">
          <h2 class="title-li"><%= todoById.title %></h2>
          <p><%= todoById.text %></p>
        </div>
      </div>
      <div>
        <form action="/todo/<%= todoById.id %>" method="post">
          <button>Delete</button>
        </form>
        <a class="button" href="/edit-todo/<%= todoById.id %>">
          <button>Edit</button></a
        >
      </div>
    </div>
  </body>
</html>
